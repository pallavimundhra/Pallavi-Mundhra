package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Light Pastel Palette (White, Baby Blue & soft accents)
val BabyBlue = Color(0xFF9CCAFF) // Relaxing Pastel Baby Blue
val BabyBlueDark = Color(0xFF5D99E0) // Deep tone for text/icons
val SoftLavender = Color(0xFFE1E7FF) // Muted lavender overlay
val PastelPink = Color(0xFFFFE0E6) // Skin rose pink tone
val PastelGreen = Color(0xFFD0F4DE) // Calming ingredient green
val PureWhite = Color(0xFFFFFFFF) // Primary canvas
val SoftCream = Color(0xFFF9FAFF) // Muted background canvas
val SlateCharcoal = Color(0xFF1E293B) // High legibility dark text
val SlateMuted = Color(0xFF64748B) // Subdued body text
val IceBlue = Color(0xFFE8F1FC) // Light container accent
val ErrorCoral = Color(0xFFFF8B94) // Muted coral error

// Dark Theme counterparts
val BabyBlueDarkTheme = Color(0xFF1E293B)
val MutedBlueDarkTheme = Color(0xFF0F172A)
val IceBlueDarkTheme = Color(0xFF334155)
